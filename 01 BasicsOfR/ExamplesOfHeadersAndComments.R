library(datasets)

# Example title ####

## A subtitle ####

mn <- mean(iris$Sepal.Length) # this is a comment
max <- max(iris$Sepal.Length)

# Example title 2 ####

model <- lm(iris$Sepal.Length, iris$Petal.Length)

## A subtitle #################################################################

### A subsubtitle #############################################################